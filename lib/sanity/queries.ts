/**
 * Barrel export — re-export tất cả services để component dùng 1 đường dẫn duy nhất.
 *
 * Usage trong component:
 *   import { fetchGalleryImages } from '@/lib/sanity/queries'
 */
export { fetchGalleryImages } from './services/gallery';
